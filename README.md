# 🌐 Web Proxy - Cloudflare Workers

یک وب پراکسی قدرتمند و سریع ساخته شده با Cloudflare Workers که به شما امکان می‌دهد به هر سایتی دسترسی داشته باشید.

## ✨ ویژگی‌ها

- ✅ **پراکسی کامل HTML/CSS/JavaScript** - بازنویسی کامل تمام لینک‌ها و منابع
- ✅ **مدیریت کوکی‌ها** - نگهداری session و login سایت‌ها
- ✅ **پشتیبانی از AJAX/Fetch** - کار با سایت‌های مدرن مثل یوتیوب، توییتر
- ✅ **مدیریت فرم‌ها** - submit کردن فرم‌ها و جستجو
- ✅ **رابط کاربری فارسی** - صفحه اصلی و جستجو به فارسی
- ✅ **سریع و رایگان** - استفاده از شبکه جهانی Cloudflare
- ✅ **بدون نیاز به سرور** - Serverless Architecture

## 🚀 نصب و راه‌اندازی

### پیش‌نیازها

1. Node.js (نسخه 16 یا بالاتر)
2. یک اکانت Cloudflare (رایگان)

### مراحل نصب

1. **Clone کردن پروژه:**
```bash
git clone <repository-url>
cd web_proxy
```

2. **نصب Wrangler CLI:**
```bash
npm install -g wrangler
```

3. **Login به Cloudflare:**
```bash
wrangler login
```

4. **Deploy کردن Worker:**
```bash
npx wrangler deploy
```

5. **آماده است! 🎉**
Worker شما روی آدرس `https://webproxy.your-subdomain.workers.dev` در دسترس است.

## 📖 نحوه استفاده

### روش 1: از طریق صفحه اصلی
1. به آدرس worker خود بروید
2. URL سایت مورد نظر را وارد کنید
3. روی "ورود به سایت" کلیک کنید

### روش 2: مستقیم در URL
```
https://your-worker.workers.dev/https://www.example.com
```

### روش 3: جستجو
```
https://your-worker.workers.dev/search?q=your+query
```

## 🎯 مثال‌های کاربردی

```bash
# دسترسی به یوتیوب
https://your-worker.workers.dev/https://www.youtube.com

# دسترسی به توییتر
https://your-worker.workers.dev/https://twitter.com

# جستجو در گوگل
https://your-worker.workers.dev/search?q=cloudflare+workers
```

## ⚙️ تنظیمات پیشرفته

برای اطلاعات بیشتر درباره تنظیمات Cloudflare Dashboard، فایل [CLOUDFLARE-SETTINGS.md](CLOUDFLARE-SETTINGS.md) را مطالعه کنید.

### تنظیمات Local

فایل `wrangler.jsonc`:
- `name`: نام worker
- `compatibility_date`: تاریخ سازگاری API
- `compatibility_flags`: فیچرهای اضافی
- `limits.cpu_ms`: محدودیت CPU (برای اکانت‌های پولی)

## 📁 ساختار پروژه

```
web_proxy/
├── worker.js              # کد اصلی worker
├── wrangler.jsonc        # تنظیمات Cloudflare
├── README.md             # این فایل
├── CLOUDFLARE-SETTINGS.md # راهنمای تنظیمات
└── LICENSE               # لایسنس پروژه
```

## 🔧 توسعه و سفارشی‌سازی

### تغییر صفحه اصلی
تابع `getHomePage()` در `worker.js` را ویرایش کنید.

### افزودن User-Agent جدید
آرایه `userAgents` در `worker.js` را ویرایش کنید.

### تست Local
```bash
npx wrangler dev
```

سپس به `http://localhost:8787` بروید.

## 🐛 عیب‌یابی

### مشکلات رایج

**1. خطای "Exceeded CPU time"**
- به برنامه Workers Paid ارتقا دهید
- یا محدودیت CPU را در `wrangler.jsonc` افزایش دهید

**2. کوکی‌ها کار نمی‌کنند**
- مطمئن شوید browser در حالت Incognito نیست
- Cookie settings را چک کنید

**3. برخی سایت‌ها کار نمی‌کنند**
- ممکن است از WebSocket یا فناوری‌های خاص استفاده کنند
- بعضی سایت‌ها proxy را block می‌کنند

## 📊 محدودیت‌ها

### برنامه رایگان Cloudflare:
- 100,000 request در روز
- 10ms CPU time هر request
- 128MB memory

### برنامه پولی:
- نامحدود request
- 50ms CPU time
- 128MB memory

## ⚠️ هشدارها

- این ابزار فقط برای استفاده شخصی و آموزشی است
- از آن برای کارهای غیرقانونی استفاده نکنید
- Cloudflare Terms of Service را رعایت کنید
- مسئولیت استفاده بر عهده کاربر است

## 🤝 مشارکت

این پروژه open-source است و مشارکت شما را می‌پذیرد!

## 📜 لایسنس

[MIT License](LICENSE)

## 🙏 تشکر

- Cloudflare Workers برای پلتفرم عالی
- جامعه open-source

---

**ساخته شده با ❤️ در ایران**

