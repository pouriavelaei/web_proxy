# تنظیمات Cloudflare Workers

## تنظیمات لازم در Dashboard

### 1. استقرار Worker
Worker شما به صورت خودکار با دستور زیر deploy می‌شود:
```bash
npx wrangler deploy
```

### 2. تنظیمات پیشنهادی در Cloudflare Dashboard

بعد از deploy، برای بهینه‌سازی عملکرد به `Workers & Pages` > worker خودتان بروید:

#### ⚙️ Settings > General

**CPU Limits:**
- اگر سایت‌های پیچیده (مثل یوتیوب، توییتر) را proxy می‌کنید
- ممکن است به CPU بیشتری نیاز داشته باشید
- در برنامه رایگان محدودیت 10ms است
- در برنامه پولی می‌توانید تا 50ms افزایش دهید

**Usage Model (برای اکانت‌های Paid):**
- اگر ترافیک زیادی دارید
- `Unbound` را فعال کنید (به جای Bundled)
- هزینه بر اساس مدت زمان CPU است نه تعداد request

#### 🔒 Settings > Variables and Secrets

**فعلاً نیازی نیست** - worker شما از environment variable استفاده نمی‌کند

#### 🌍 Settings > Triggers (Routes)

**به صورت پیش‌فرض:**
- Worker روی آدرس `*.workers.dev` در دسترس است
- مثال: `https://webproxy.your-subdomain.workers.dev`

**اگر می‌خواهید دامین شخصی:**
1. یک دامین در Cloudflare اضافه کنید
2. در بخش Triggers > Routes یک route اضافه کنید:
   - مثال: `proxy.yoursite.com/*`
3. DNS را تنظیم کنید (به صورت خودکار انجام می‌شود)

#### 🔐 CORS & Security Headers

**خبر خوب:** Worker شما همه header های امنیتی را خودکار مدیریت می‌کند:
- ✅ CORS headers
- ✅ Cookie handling  
- ✅ CSP removal (برای سایت‌های مقصد)

#### 📊 Monitoring

برای مشاهده آمار و خطاها:
- به `Workers & Pages` > worker خودتان > `Logs` بروید
- یا از `Real-time Logs` استفاده کنید

## محدودیت‌های برنامه رایگان

| محدودیت             | مقدار   |
| ------------------- | ------- |
| Request ها در روز   | 100,000 |
| CPU time هر request | 10ms    |
| Memory              | 128MB   |
| Script size         | 1MB     |

**توجه:** برای proxy کردن سایت‌های سنگین ممکن است به CPU بیشتری نیاز داشته باشید.

## مشکلات احتمالی و راه حل

### 1. خطای "CPU time limit exceeded"
**راه حل:**
- برنامه Workers Paid را فعال کنید
- یا سایت‌های کم‌حجم‌تر را proxy کنید

### 2. خطای "Script size too large"
**راه حل:**
- کد HTML صفحه اصلی را کوچک‌تر کنید
- Minify کنید

### 3. مشکل در کوکی‌ها
**راه حل:**
- مطمئن شوید browser cookies را block نکرده
- SameSite=None به صورت خودکار تنظیم می‌شود

## تست کردن

بعد از deploy:
1. به آدرس worker خود بروید
2. یک URL تست کنید (مثلاً `youtube.com`)
3. بررسی کنید:
   - ✅ صفحه به درستی لود می‌شود
   - ✅ لینک‌ها کار می‌کنند
   - ✅ فرم‌ها submit می‌شوند
   - ✅ کوکی‌ها ذخیره می‌شوند

## نکات امنیتی

⚠️ **هشدار:** این proxy برای دسترسی به سایت‌های محدود شده است. 
- از آن برای کارهای غیرقانونی استفاده نکنید
- Cloudflare ممکن است worker شما را disable کند
- برای استفاده شخصی مناسب است

## پشتیبانی

اگر مشکلی پیش آمد:
1. Logs را در Dashboard چک کنید
2. مطمئن شوید worker آخرین نسخه را دارد
3. Browser cache را پاک کنید
